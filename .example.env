# 在项目根目录启动时自动加载 .env；已有环境变量优先。
# 填写服务商的 API base URL 和 key，不要提交 .env。
LHAGENT_BASE_URL=
LHAGENT_API_KEY=

# 可选项；不设置时使用默认值。
# LHAGENT_TIMEOUT_SECONDS=60
# LHAGENT_MAX_RETRIES=2
# LHAGENT_MAX_RETRY_DELAY_SECONDS=30
